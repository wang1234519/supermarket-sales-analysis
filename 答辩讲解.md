# 超市销售分析平台 —— 答辩讲解手册

> 配套文档：《代码详解.md》（每一行代码的作用，逐文件讲解）
> 建议顺序：先读本手册把"故事"讲顺，再对照代码详解把"细节"吃透。

---

## 一、一分钟开场白（可照着讲）

> 各位老师好，我做的项目是**超市销售数据分析平台**。
>
> 项目背景是：一家连锁超市在全国 5 个城市开了 18 家门店，半年产生了约 **26.8 万行订单明细**，数据量已经超过单机表格软件能轻松处理的范围，管理层想知道——**哪个品类最赚钱、一天中什么时候是销售高峰、半年生意是涨是跌、哪家门店经营最好**。
>
> 我按照大数据标准流程完成了整条链路：原始数据上传 **HDFS** 存储 → 在 **Hive** 中建仓、清洗、做多维统计分析，产出 **16 张结果表** → 用 **Sqoop** 把结果表导出到本地 **MySQL** → 最后用 **SpringBoot + MyBatis** 只读取结果表，配合 **ECharts** 做成了 8 个页面的可视化分析平台，并实现了**登录认证**（密码 BCrypt 加密）、**结果表数据浏览**和**图表导出**功能。
>
> 下面我来演示。

## 二、架构图（讲这张图，答辩重点）

```
                 ┌──────────── 虚拟机（Hadoop 集群）────────────┐
8个txt原始文件 ─▶ │ HDFS 存储 ─▶ Hive 建仓(建3张原始表) ─▶      │
(门店/商品/6个月  │ Hive SQL 清洗 + 多维统计 ─▶ 16 张结果表      │
 订单，约26.8万行)│                │ Sqoop export              │
                 └────────────────┼────────────────────────────┘
                                  ▼
              本地 MySQL（supermarket_sales 库）
              ├─ 16 张 result_* 结果表（只读）
              └─ sys_user 用户表（登录认证）
                                  │
                                  ▼
        SpringBoot + MyBatis（只读结果表，不做计算）
              ├─ 登录认证：Session + 拦截器 + 过滤器 + BCrypt
              ├─ REST 接口：22 个分析/数据/认证接口
                                  │ JSON
                                  ▼
        前端 8 个页面（原生 HTML/CSS/JS + ECharts 5.6）
        登录/注册 + 总览 + 品类 + 时段 + 月度 + 门店 + 商品 + 结果数据
```

**一句话串讲**：存储用 HDFS、计算用 Hive、搬运用 Sqoop、结果落 MySQL、展示用 SpringBoot + ECharts——五个环节正好对应大数据处理的标准流程"采集 → 存储 → 计算 → 分析 → 可视化"。

## 三、技术选型理由（老师必问"为什么"）

| 技术 | 为什么用它 |
|---|---|
| HDFS | 数据量大、单机表格软件打开都吃力；HDFS 分布式存储、多副本自动容错 |
| Hive | 能把 SQL 自动翻译成 MapReduce 在集群并行执行，会 SQL 就能做大数据分析，不用手写分布式程序 |
| Sqoop | 官方批同步工具，一条命令在 Hive/MySQL 之间搬表，本项目离线分析对时效要求不高，正合适 |
| MySQL 存结果表 | 结果表很小（最大 702 行），放关系库供业务系统直接读取，是"结果落地"的标配 |
| SpringBoot + MyBatis | 快速搭建 Web 服务；MyBatis 用 XML 写 SQL，分析查询逻辑清晰、可读性强、便于调优 |
| 结果表模式（不实时算） | Hive 离线算好，展示端只读结果表——查询快、职责分离，展示层不依赖大数据集群也能跑 |
| ECharts | 功能全（折线/柱状/饼图/热力图）、开源免费、本地引入无需联网 |
| 登录用 Session 而非 JWT | 传统 B/S 项目标准做法，简单可靠；BCrypt 单向加密存密码 |

## 四、答辩演示路线（按这个顺序点，边点边讲）

1. **登录页**：输错密码 → 提示"用户名或密码错误"（讲 BCrypt 比对）；正确登录 → 进入总览
   （可顺带讲：未登录直接访问接口会 401、访问页面会跳登录页，这是拦截器+过滤器）
2. **数据总览**：先讲 6 张 KPI 卡（总销售额 5700 万+、订单 8.5 万+、客单价 669 元）；
   再讲 4 张核心图，每张对应一个业务问题：
   - 月度趋势 → "生意越来越好还是变差"（4 月环比 +2.18%）
   - 品类占比 → "哪个品类最赚钱"（清洁护理 21%+ 第一）
   - 时段分布 → "什么时间是高峰"（图中橙色高亮高峰时段）
   - 门店 TOP10 → "哪家店最好"（横向条形图）
3. **品类分析**：点图例筛选品类 → 讲品类×月份多系列趋势；选品类看热销商品表
4. **时段分析**：讲热力图（星期×时段，颜色越深销售额越高）、工作日 vs 周末对比
5. **月度趋势**：讲环比增长柱状图（涨绿跌红）、点选某月看每日走势（含日均线）
6. **门店分析**：讲城市消费力（深圳 6 店第一）、下拉看单店经营详情
7. **商品分析**：畅销 TOP10 / 滞销 TOP10（滞销→建议促销下架，对应课程思考题）
8. **结果数据**：下拉切换 16 张结果表 → 强调"这些就是 Hive 算好、Sqoop 导出来的结果表，
   平台只读展示"——这一步能证明你整条链路是真的
9. （可选）点图表右上角"导出图片"按钮，现场导出 PNG
10. **退出登录** → 再访问接口演示 401

## 五、高频追问 40 问（附答案）

### 架构类

1. **整个项目的数据链路是什么？** HDFS 存原始数据 → Hive 建仓+SQL 分析 → 16 张结果表 → Sqoop 导出 → 本地 MySQL → SpringBoot+MyBatis 读取 → ECharts 展示。
2. **为什么项目里 SpringBoot 不做计算，只读结果表？** 计算在数据量大、口径复杂时属于离线分析场景，由 Hive 在集群并行完成；展示层只读结果表，职责单一、响应快，而且展示服务与大数据集群解耦——集群关了报表也能看。
3. **五个环节分别用的什么技术？** 存储 HDFS、计算 MapReduce（Hive 底层自动翻译）、分析 Hive SQL、迁移 Sqoop、可视化 SpringBoot+ECharts。
4. **数据量算"大数据"吗？** 26.8 万行对 Hadoop 来说不大，但对单机表格软件已经吃力；这个项目是**流程演练**——把真实大数据场景的标准链路完整走一遍，数据量放大到亿级，流程不变。
5. **如果数据量是 10 亿行，你的项目哪里会变？** 前端和后端不变（读结果表）；变的是 Hive 侧——SQL 不变，集群加节点/调资源即可；Sqoop 导出可以用 `--num-mappers` 并行、分批导出。

### Hive 类

6. **Hive 是什么？和 MySQL 有什么区别？** Hive 是数据仓库工具，SQL 会翻译成 MapReduce 在集群并行执行，适合大规模离线分析；MySQL 是关系数据库，适合在线事务和小结果集。本项目里 Hive 算、MySQL 存结果。
7. **维度表和事实表有什么区别？** 维度表描述"谁、在哪"（门店、商品），数据小、变化慢；事实表记录"发生了什么"（订单明细），数据大、持续增长。星型模型就是事实表通过外键关联多个维度表。
8. **三张表怎么关联？** `fact_order_detail.product_id = dim_product.product_id` 关联商品，`store_id = dim_store.store_id` 关联门店。
9. **品类占比怎么算的？** `sum(amount) / sum(sum(amount)) over() * 100`——窗口函数在整个结果集上再求一次总销售额，每行都能拿到全局总数算占比。
10. **环比增长率怎么算的？** 子查询按月分组后，用 `lag(sales,1) over(order by month)` 取上一个月的销售额，`(本月-上月)/上月*100`；首月没有上月所以是 NULL。
11. **星期怎么算出来的？** `pmod(datediff(to_date(order_time),'2026-01-05'),7)+1`。2026-01-05 是周一，日期差对 7 取模，1=周一…7=周日。pmod 保证负数也能取到正余数。
12. **滞销商品怎么定义？** 商品表 LEFT JOIN 订单表，没卖过的商品销售额为 0；按销售额升序取前 10 就是滞销。LEFT JOIN 是为了把"零销售"的商品也纳入统计。
13. **INSERT OVERWRITE 和 INSERT INTO 区别？** OVERWRITE 覆盖整表，重复执行不会产生重复数据；INTO 追加。分析脚本全部用 OVERWRITE，所以可以放心重跑。

### Sqoop 类

14. **Sqoop 是干什么的？** Hadoop 生态官方工具，一条命令在 HDFS/Hive 和关系数据库之间搬数据。导出（export）是 Hive→MySQL。
15. **为什么选 Sqoop 不用 Flume/CDC？** 三种方案：日志采集（Flume+Kafka+Flink）适合实时流、CDC（Debezium）实时性最强但依赖数据库日志、Sqoop 最简单适合离线批量。本项目离线分析、时效要求不高，Sqoop 一条命令搞定。
16. **Sqoop 导出的原理是什么？** export 本质是启动一个 MapReduce 任务：把 HDFS 上 Hive 表目录下的数据文件按行读取、按字段分隔符（\001）切分，生成 INSERT 语句批量写入 MySQL。
17. **为什么脚本里要先 TRUNCATE 再导出？** export 是追加写入，重复执行会重复；先清空目标表保证幂等。
18. **--input-fields-terminated-by '\001' 是什么意思？** Hive 表默认用 \001（^A）作字段分隔符，告诉 Sqoop 按这个字符切列。
19. **--input-null-string '\\N' 是干嘛的？** Hive 中 NULL 存成字符串 "\N"，Sqoop 读到 "\N" 时把它当 NULL 写入 MySQL，否则 DECIMAL 列会因无法解析报错。
20. **Sqoop 1.4.7 + Hadoop 3.3 踩过什么坑？** ① 缺 commons-lang-2.6 报 NoClassDefFoundError，从 Hive lib 拷 jar 解决；② HADOOP_CONF_DIR 没配会报 Cannot initialize Cluster；③ 不用 hcatalog 而用 --export-dir 直接读 HDFS 文件，兼容性最好。

### 数据库 / 结果表类

21. **MySQL 里有几张表？** 17 张：16 张 result_* 结果表 + 1 张 sys_user 用户表。
22. **为什么是 16 张结果表而不是 4 张？** 课程核心要求 4 个维度（品类/时段/月度/门店），我又扩展了城市、商品、星期、热力图、每日走势、门店下钻等维度让页面更丰富——每张结果表对应一个页面模块，Hive 侧各自只有一句 GROUP BY。
23. **结果表有索引吗？** 结果表最大才 702 行，全表扫描也毫秒级，不需要索引；只有用户表 username 建了唯一索引（登录查询+防重）。
24. **结果表的数据一致性怎么保证？** 展示端只读；数据更新由"重跑 Hive 分析 + 重跑导出脚本"完成，脚本先清空再写入，不会脏数据。
25. **schema.sql 每次启动都执行会不会丢数据？** 不会，全部 CREATE TABLE IF NOT EXISTS，只建不删不覆盖，幂等。

### SpringBoot / MyBatis 类

26. **项目分了哪几层？各管什么？** Controller 接请求、Service 业务逻辑、Mapper 数据访问（XML 写 SQL）、DTO 传输对象。分层的好处：职责单一、易维护、易测试。
27. **MyBatis 和 MyBatis-Plus 为什么选前者？** 课程要求 + 统计 SQL 用 XML 写可读性最好，也更贴近"会写 SQL 就能做分析"的主线。
28. **#{param} 和 ${table} 有什么区别？为什么结果数据页用 ${}？** #{} 是预编译占位符，值会被转义防注入；${} 是字符串拼接。表名不能用占位符，所以必须用 ${}——因此我在 Service 层做了 16 张表的**白名单校验**，表名不在白名单直接抛异常，杜绝 SQL 注入。
29. **前端请求一个接口的完整流程？** 浏览器 fetch → AuthInterceptor 校验会话 → Controller 收参 → Service 处理 → Mapper 执行 SQL → MySQL 返回行 → MyBatis 映射成 DTO（下划线转驼峰）→ Jackson 序列化 JSON → 前端渲染。
30. **map-underscore-to-camel-case 是干嘛的？** 把数据库下划线列名（total_sales）自动映射到 Java 驼峰属性（totalSales），省去手写别名。
31. **Lombok 是干嘛的？** @Data 自动生成 getter/setter/toString，@RequiredArgsConstructor 生成构造器注入，减少样板代码。JDK 24 下需要在 pom 显式配置注解处理器路径，否则编译期不生成代码。
32. **为什么用 DTO 而不是直接用 Map？** 强类型、字段明确、编译期能发现拼写错误，前后端契约清晰。

### 登录认证类（重点）

33. **登录认证的完整流程？** 前端提交账号密码 → AuthController.login → AuthService 按用户名查库 → BCryptPasswordEncoder.matches() 比对密文 → 通过则把用户信息（不含密码）放进 HttpSession → 返回成功。
34. **密码怎么存的？** 数据库里是 BCrypt 密文（$2a$10$...）。BCrypt 是单向哈希+随机盐，同一个人两个相同密码会生成不同密文，无法反推、不能彩虹表破解。
35. **为什么不用明文/加盐 MD5？** MD5 可暴力破解；BCrypt 自带盐且计算慢（故意慢），是业界标准。
36. **拦截器和过滤器的区别？本项目怎么分工？** 过滤器是 Servlet 规范，作用于所有请求，项目里用它把未登录的页面请求 302 重定向到 login.html；拦截器是 Spring MVC 的，只作用于 Controller 请求，用它给未登录的 /api 接口统一返回 401 JSON。分工：页面看过滤器、接口看拦截器。
37. **哪些接口不用登录？** /api/auth/login、/api/auth/register、/api/auth/me 三个（me 用于前端判断会话是否有效）。
38. **会话超时后会发生什么？** 30 分钟无操作 Session 失效；此时再点接口收到 401，前端自动跳回登录页。
39. **注册时用户名重复怎么处理？** 先查库，存在就抛"用户名已存在"；同时 username 有唯一索引兜底。
40. **为什么默认管理员是启动时初始化而不是写死在 SQL 里？** 密文要用 BCrypt 动态生成；ApplicationRunner 里判断用户表为空才插入 admin，幂等且密码不落明文。

### 前端类

41. **前端用了什么框架？** 没有框架，原生 HTML/CSS/JS + ECharts——项目重点是数据链路和后端，前端保持轻量；fetch 调 REST 接口拿 JSON，图表交给 ECharts。
42. **前端怎么知道该跳登录页？** common.js 的 api() 封装统一处理：响应码 401 就 location 跳 login.html；页面加载时还会调 /api/auth/me 渲染顶部用户名。
43. **热力图怎么做的？** ECharts heatmap 类型 + visualMap 组件：x 轴 24 小时、y 轴星期、值映射成颜色深浅（白→深蓝）。
44. **商品页为什么前端排序而不是后端分页？** 结果表只有 39 行，一次取全量，搜索/筛选/排序全在前端内存完成，响应即时、后端简单。
45. **图表导出图片怎么实现的？** ECharts 自带 toolbox.saveAsImage，common.js 的 initChart() 统一给每个图表注入这个工具栏。

### 开放题 / 课程思考题

46. **哪个城市消费力最强？怎么算？** 深圳最强（33%）。门店表关联订单，按 city 分组 sum(amount) 排序。已在门店分析页实现。
47. **哪些商品滞销该下架？** 按销量升序——"其他-散装商品"销售额最低。商品页有滞销 TOP10。
48. **这个项目还能怎么扩展？** 加入实时数据（Flume+Kafka+Flink 流处理）、增加 ETL 数据清洗环节（去重、补全、类型校验）、做用户画像/购物篮关联分析（哪些商品经常一起买）、增加操作日志与用户管理、图表自动轮播大屏等。
49. **如果让你重新做一次，会改进什么？** ① 原始数据加 ETL 清洗步骤（目前模拟数据较干净）；② 结果表用宽表/视图减少表数量；③ 前后端分离用 Vue；④ 接口加限流和操作审计。
50. **这个项目最大的难点是什么？** ① 把课程 Hadoop 流程落地到可演示的完整闭环（Hive 16 张结果表 + Sqoop 导出 + 展示层对接）；② 跨环境联调：虚拟机→宿主机 MySQL 的网络/账号/防火墙配置；③ Sqoop 1.4.7 与 Hadoop 3.3 的兼容性问题排查。

## 六、可能被挑的毛病 & 应对

| 可能的质疑 | 应对 |
|---|---|
| "数据量这么小，用 Hadoop 是杀鸡用牛刀" | 明确说这是流程演练：26.8 万行 Excel 都卡，且课程要求走完整大数据链路；流程对大数据的正确性已在架构上保证（见第 4、5 问） |
| "结果表都是 Hive 算的，SpringBoot 没什么技术含量" | 展示层的价值在：22 个只读接口的抽象设计、登录认证安全体系、防注入白名单、前后端契约；而且"只读结果表"正是架构上的正确选择（见第 2 问） |
| "为什么不用 Vue/React" | 项目核心是数据链路，前端保持轻量可控；ECharts 已覆盖全部可视化需求（如实回答，不必辩解） |
| "登录为什么不用 JWT" | Session 是传统 B/S 项目标准做法，实现简单可靠；JWT 适合前后端完全分离/多端场景，本项目页面和接口同域，Session 足够 |
| "Sqoop 都报错过哪些问题" | 把踩坑经历讲出来反而是加分项：commons-lang 冲突、Cluster 初始化、hcatalog 兼容性——证明真的动手做过 |

## 七、答辩前一天 Checklist

- [ ] 虚拟机 Hadoop 集群能正常启动，`hive -e "SELECT COUNT(*) FROM supermarket_dw.fact_order_detail"` = 267822
- [ ] 本地 MySQL 服务已启动，16 张结果表有数据
- [ ] IDEA 能启动项目，浏览器登录 admin 后 8 个页面全部有图
- [ ] 演示前把浏览器开好、提前登录，避免现场等待
- [ ] 准备好"演示时网断/虚拟机没开"的预案：直接展示本地页面和 MySQL 数据，说明链路脚本在 hive/ 目录
- [ ] 把《代码详解.md》里认证、Mapper、Hive 三类各挑 3 个文件精读，能指着代码讲
