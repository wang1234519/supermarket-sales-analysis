# 超市销售分析平台 —— 项目部署文档

> 适用对象：需要在本机部署并运行本项目的同学/老师。
> 本文档提供两条部署路径：
> - **路径 A（快速部署）**：只需 JDK + MySQL，直接导入现成的分析结果数据即可看到全部报表（约 10 分钟）；
> - **路径 B（完整大数据链路）**：自建 Hadoop 集群，走 HDFS → Hive 分析 → Sqoop 导出全流程（见第三章，约 1~2 天）。

---

## 一、环境要求

| 软件 | 版本要求 | 用途 | 说明 |
|---|---|---|---|
| JDK | 17+（推荐 24） | 编译运行后端 | 按项目 pom 配置为 24 |
| MySQL | 8.x | 存放结果表与用户表 | 数据库名 `supermarket_sales`（可自动创建） |
| Maven | 3.6+（或使用项目自带 `mvnw` 包装器，免安装） | 构建项目 | IDEA 自带 Maven 亦可 |
| IDEA（推荐） | 2023+ | 开发/运行环境 | 也可纯命令行运行 |
| 浏览器 | Chrome / Edge 等 | 访问页面 | ECharts 本地引入，**无需联网** |

> 路径 B 额外需要：VMware + CentOS 7 虚拟机、Hadoop 3.3.0、Hive 3.1.3、Sqoop 1.4.7（课程 Docker 镜像环境）。

---

## 二、路径 A：快速部署（推荐，无需大数据环境）

### 第 1 步：安装 JDK 并确认版本

```bash
java -version
```

输出 17 及以上版本即可（项目按 24 开发验证）。

### 第 2 步：安装并启动 MySQL 8

- Windows 安装 MySQL 8.0（安装时记住 root 密码）；
- 确认服务已启动（`net start MySQL80` 或服务管理器）；
- 验证登录：

```bash
mysql -uroot -p
```

### 第 3 步：获取项目代码

将项目目录整体拷贝到目标机器（例如 `D:\practice\`），无需额外下载依赖——首次构建时 Maven 自动下载。

### 第 4 步：修改数据库配置

编辑 `src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    username: root          # 改成目标机器 MySQL 账号
    password: 你的MySQL密码   # 改成目标机器 MySQL 密码
```

> 数据库 `supermarket_sales` 不需要手工创建，连接串已带 `createDatabaseIfNotExist=true`，首次启动自动建库。

### 第 5 步：建库建表

二选一：

- **方式 A（推荐）**：直接跳到第 7 步启动项目，应用启动时会自动执行 `schema.sql` 创建 17 张表（16 张结果表 + 用户表）；
- **方式 B（手动）**：在 MySQL 客户端执行 `sql/init.sql`（含建库 + 建表语句）。

### 第 6 步：导入分析结果数据

> 说明：本项目的所有报表数据是**在 Hive 中计算好的结果表**（项目本身不做计算）。这里提供的 `sql/data.sql` 就是已算好的结果数据（2026 年上半年 16 张结果表，共约 1400 行），导入后即可看到全部图表；想要体验完整大数据计算链路请走第三章路径 B。

命令行导入（需先完成第 5 步建表）：

```bash
mysql -uroot -p --default-character-set=utf8mb4 supermarket_sales < sql/data.sql
```

导入后验证（应看到 16 张表都有数据，总销售额为 57084290.95）：

```sql
USE supermarket_sales;
SELECT * FROM result_kpi;
SELECT COUNT(*) FROM result_store_sales;        -- 18
SELECT COUNT(*) FROM result_heatmap_sales;      -- 98
```

> 也可以直接用 Navicat / DataGrip 打开 `sql/data.sql` 执行。

### 第 7 步：启动项目

**方式 A：IDEA 启动（推荐）**
1. File → Open 打开项目目录，等待 Maven 依赖下载完成；
2. 若 Lombok 报红：Settings → Build → Compiler → Annotation Processors → Enable；
3. 运行 `com.supermarket.SupermarketApplication`；
4. 控制台出现 `Started SupermarketApplication` 即启动成功。

**方式 B：命令行启动（免 IDEA）**

```bash
# Windows cmd 用 mvnw.cmd，Git Bash 用 ./mvnw
mvnw spring-boot:run
```

或打包后运行（生成 target/supermarket-sales-analysis-1.0.0.jar）：

```bash
mvnw package -DskipTests
java -jar target/supermarket-sales-analysis-1.0.0.jar
```

### 第 8 步：验证部署

1. 浏览器打开 http://localhost:8080 → 自动跳转登录页；
2. 使用默认账号登录：**admin / admin123**（首次启动自动创建，也可点"立即注册"创建新账号）；
3. 按以下清单逐页验证：

| 页面 | 应看到的内容 |
|---|---|
| 数据总览 | 6 张 KPI 卡（总销售额 ¥5708.4万等）+ 4 张图表 |
| 品类分析 | 品类排行、占比环形图、品类趋势折线 |
| 时段分析 | 24 小时分布、星期对比、热力图（颜色深浅） |
| 月度趋势 | 双轴图、环比柱状图（涨绿跌红）、每日走势 |
| 门店分析 | 城市消费力、18 店排行榜、单店详情 |
| 商品分析 | 畅销/滞销 TOP10、商品明细表 |
| 结果数据 | 16 张结果表可切换分页浏览 |

4. 若页面顶部出现黄色横幅"结果表还没有数据"，说明第 6 步未导入成功，检查 data.sql 是否执行、库名是否一致。

---

## 三、路径 B：完整大数据链路部署（进阶）

本路径在虚拟机中重现"原始数据 → Hive 分析 → Sqoop 导出"全流程，与项目自带的三个脚本配套。

### 3.1 环境准备（课程 Docker 镜像）

VMware + CentOS 7 虚拟机，导入课程 Docker 镜像，`docker compose up -d` 启动 3 节点 Hadoop 集群（含 Hive 3.1.3）。宿主机（Windows）需安装 MySQL 8 并保证虚拟机可访问。

### 3.2 宿主机开放 MySQL 远程访问（一次性配置）

```sql
-- 在宿主机 MySQL 中执行
CREATE USER 'root'@'192.168.88.%' IDENTIFIED BY '你的密码';
GRANT ALL PRIVILEGES ON *.* TO 'root'@'192.168.88.%' WITH GRANT OPTION;
FLUSH PRIVILEGES;
```

并在 Windows 防火墙放行 3306 入站（管理员 cmd）：

```bash
netsh advfirewall firewall add rule name="MySQL 3306 (VMware NAT)" dir=in action=allow protocol=TCP localport=3306 remoteip=192.168.88.0/24
```

### 3.3 虚拟机中执行数据处理

1. 把 8 个 txt 数据文件传到虚拟机（如 `/root/data/`）；
2. 装载原始数据（改好 `hive/load-data.sql` 中路径后执行）：

```bash
hive -f load-data.sql        # 验证 fact_order_detail 应为 267822 行
```

3. 执行分析，产出 16 张结果表：

```bash
hive -f hive-analysis.sql
```

4. 修改 `hive/sqoop-export.sh` 开头的 `HOST_IP`（宿主机 VMnet8 地址，`ipconfig` 查看，通常 192.168.88.1）与 MySQL 账号密码，执行导出：

```bash
bash sqoop-export.sh
```

脚本会先检查 Hive 结果表行数、清空 MySQL 目标表（防重复）、再逐表导出，最后打印完成提示。

5. 宿主机验证 MySQL 中 16 张结果表已有数据后，按第二章第 4~8 步启动项目即可。

> Sqoop 首次使用常见问题：缺 `commons-lang-2.6`（从 Hive lib 复制到 Sqoop lib）、
> `Cannot initialize Cluster`（sqoop-env.sh 配置 HADOOP_CONF_DIR）。详见 `hive/sqoop-export.sh` 内置提示。

---

## 四、打包与分发

```bash
# 打包（跳过测试），产物在 target/ 下
mvnw package -DskipTests

# 换端口运行（默认 8080）
java -jar target/supermarket-sales-analysis-1.0.0.jar --server.port=8081
```

分发给他人时，建议将以下内容打包为 zip：

```
项目根目录/
├── src/                      # 源代码
├── sql/                      # init.sql（建表）+ data.sql（结果数据）
├── hive/                     # 大数据侧脚本（可选，路径 B 用）
├── pom.xml + mvnw(.cmd)      # 构建文件
└── 部署文档.md                # 本文档
```

`target/`、`.idea/` 无需分发（构建/IDE 自动生成）。

---

## 五、常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 启动报 `Access denied for user` | application.yml 密码与 MySQL 不一致 | 修改 yml 中 `password` |
| 启动报端口占用 | 8080 被占用 | 换端口：`--server.port=8081` |
| 页面顶部黄条"结果表还没有数据" | 结果表未导入数据 | 按第二章第 6 步执行 `sql/data.sql`（或路径 B 导出） |
| 访问页面 302 跳到登录页 | 正常现象（未登录） | 用 admin/admin123 登录 |
| 接口返回 401"未登录或登录已过期" | 会话超时（30 分钟无操作） | 重新登录 |
| 忘记管理员密码 | — | MySQL 中执行 `DELETE FROM supermarket_sales.sys_user;` 后重启项目（自动重建 admin/admin123） |
| IDEA 中 Lombok 报红 | 注解处理器未开启 | Settings → Annotation Processors → Enable annotation processing |
| 中文乱码 | 客户端字符集问题 | 命令行加 `--default-character-set=utf8mb4`；页面本身已统一 UTF-8 |
| 数据重复导入 | data.sql 是 INSERT 追加 | 重新导入前先清空：`TRUNCATE TABLE 各结果表;`（或用 sqoop-export.sh，自带清空） |

---

## 六、附：交付物清单

| 文件 | 说明 |
|---|---|
| `sql/init.sql` | 建库建表脚本（16 张结果表 + 用户表） |
| `sql/data.sql` | 2026 上半年分析结果数据（16 张表，约 1400 行 INSERT） |
| `hive/load-data.sql` | 虚拟机原始数据装载脚本（路径 B） |
| `hive/hive-analysis.sql` | 虚拟机 Hive 分析脚本：产出 16 张结果表（路径 B） |
| `hive/sqoop-export.sh` | 虚拟机 Sqoop 导出脚本（路径 B，带前置检查与自动清空） |
| `README.md` | 项目总体说明 |
| `答辩讲解.md` / `代码详解.md` | 答辩辅助文档 |
| `软件实践说明书-超市销售数据分析.docx` | 软件实践说明书（Word） |
| 本文件 | 部署文档 |

**默认账号**：admin / admin123（应用首次启动自动创建，密码 BCrypt 加密存储）。
