<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.supermarket.mapper.UserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, nickname, role)
        VALUES (#{username}, #{password}, #{nickname}, #{role})
    </insert>

    <select id="selectByUsername" resultType="com.supermarket.entity.User">
        SELECT id, username, password, nickname, role, create_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="selectById" resultType="com.supermarket.entity.User">
        SELECT id, username, password, nickname, role, create_time
        FROM sys_user
        WHERE id = #{id}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM sys_user
    </select>

    <update id="updatePassword">
        UPDATE sys_user
        SET password = #{password}
        WHERE id = #{id}
    </update>
</mapper>
