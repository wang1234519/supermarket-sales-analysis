# 超市销售分析平台 —— 代码详解（答辩版）

> 本文逐文件讲解项目代码，覆盖"每一行代码是干什么的"。
> 配合《答辩讲解.md》使用：先看手册把故事讲顺，再看本文把细节吃透。

## 〇、项目文件总览

```
D:\practice\
├── pom.xml                        # Maven 依赖与构建配置
├── sql/init.sql                   # 建库建表脚本（可手工在 Navicat 执行）
├── hive/                          # 虚拟机用的脚本
│   ├── load-data.sql              #   原始数据装载（执行一次）
│   ├── hive-analysis.sql          #   Hive 分析：产出 16 张结果表
│   └── sqoop-export.sh            #   Sqoop 导出：结果表 → 本地 MySQL
└── src/main/
    ├── java/com/supermarket/
    │   ├── SupermarketApplication.java      # 启动类
    │   ├── common/GlobalExceptionHandler.java
    │   ├── config/                          # 认证与 Web 配置（4 个类）
    │   │   ├── AuthInterceptor.java         #   接口登录拦截器
    │   │   ├── AuthFilter.java              #   页面访问过滤器
    │   │   ├── WebConfig.java               #   注册拦截器/过滤器/密码加密器
    │   │   └── DataInitializer.java         #   首次启动创建 admin 账号
    │   ├── controller/                      # 接口层（7 个）
    │   │   ├── AuthController.java          #   登录/注册/改密/退出/me
    │   │   ├── DashboardController.java     #   总览
    │   │   ├── CategoryController.java      #   品类
    │   │   ├── HourController.java          #   时段
    │   │   ├── MonthlyController.java       #   月度
    │   │   ├── StoreController.java         #   门店/城市
    │   │   ├── ProductController.java       #   商品
    │   │   └── DataController.java          #   结果表数据浏览
    │   ├── entity/User.java                 # 用户实体
    │   ├── dto/                             # 请求/响应对象（14 个）
    │   ├── mapper/                          # MyBatis 接口（3 个）
    │   └── service/                         # 业务层（7 个）
    └── resources/
        ├── application.yml                  # 应用配置
        ├── schema.sql                       # 启动自动建表
        ├── mapper/                          # MyBatis XML（3 个）
        └── static/                          # 前端（9 个页面 + css/js）
```

---

# 第一部分：配置与数据库

## 1. pom.xml —— 依赖与构建

```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
</parent>
```
继承 Spring Boot 官方父工程：自动锁定几百个依赖的兼容版本、内置 Maven 插件配置，省得自己管版本冲突。

```xml
<properties><java.version>24</java.version></properties>
```
编译目标 JDK 24（本机安装的版本）。

四个核心依赖：
1. `spring-boot-starter-web`：内嵌 Tomcat + Spring MVC，提供 Web 服务能力。
2. `mybatis-spring-boot-starter`（3.0.5）：Spring Boot 官方推荐的 MyBatis 整合包，自动配置 SqlSessionFactory、扫描 mapper。
3. `mysql-connector-j`：MySQL 官方 JDBC 驱动（runtime 作用域，只在运行时需要）。
4. `spring-security-crypto`：**只**引入 Spring Security 的加密模块（不引入完整 Security 框架），用来做 BCrypt 密码加密。
5. `lombok`（optional）：@Data 等注解编译期生成 getter/setter，减少样板代码。

```xml
<maven-compiler-plugin> ... <annotationProcessorPaths>lombok...</annotationProcessorPaths>
```
JDK 23 起 javac 不再自动从 classpath 发现注解处理器，必须显式声明 Lombok 的注解处理器路径，否则编译时 @Data 不生效（本项目实际踩过的坑）。

## 2. application.yml —— 应用配置（逐行）

```yaml
server:
  port: 8080                        # 端口
  servlet:
    session:
      timeout: 30m                  # 会话 30 分钟无操作自动失效
```

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/supermarket_sales?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&rewriteBatchedStatements=true
```
JDBC 连接串参数逐个解释：
- `characterEncoding=utf8`：中文不乱码；
- `serverTimezone=Asia/Shanghai`：时区统一东八区；
- `useSSL=false`：本地开发不走 SSL；
- `allowPublicKeyRetrieval=true`：允许客户端取服务端公钥（MySQL 8 的 caching_sha2_password 认证需要）；
- `createDatabaseIfNotExist=true`：**数据库不存在时自动创建**（这就是"首次启动无需手工建库"的原理）；
- `rewriteBatchedStatements=true`：批量写入性能优化。

```yaml
    username: root
    password: root                  # 本机 MySQL 密码（改成你自己的）
```

```yaml
  sql:
    init:
      mode: always                  # 每次启动都执行 schema.sql
      schema-locations: classpath:schema.sql
      encoding: UTF-8
```
启动时自动执行 schema.sql 建表。`mode: always` 配合脚本里的 `IF NOT EXISTS` 实现幂等。

```yaml
mybatis:
  mapper-locations: classpath:mapper/*.xml   # XML 映射文件位置
  configuration:
    map-underscore-to-camel-case: true       # 数据库 total_sales → Java totalSales
```

## 3. schema.sql —— 建表脚本

应用每次启动自动执行（幂等）。共 17 张表：16 张 `result_*` 结果表 + 1 张 `sys_user`。

**以 result_store_sales 为例逐字段讲**：

```sql
CREATE TABLE IF NOT EXISTS `result_store_sales` (   -- IF NOT EXISTS = 幂等
    `store_id`         INT           COMMENT '门店编号',       -- 门店维度外键
    `store_name`       VARCHAR(50)   COMMENT '门店名称',
    `city`             VARCHAR(50)   COMMENT '所在城市',
    `sales`            DECIMAL(16,2) COMMENT '销售额（元）',   -- 汇总值，DECIMAL 避免浮点误差
    `orders`           BIGINT        COMMENT '订单数',
    `quantity`         BIGINT        COMMENT '销量（件）',
    `avg_order_amount` DECIMAL(12,2) COMMENT '客单价（元）'
) ENGINE = InnoDB COMMENT = '门店业绩结果表';
```

16 张结果表的共同规律：
- **一张结果表 = 一个分析维度**，列顺序与 Hive 结果表**完全一致**（Sqoop 导出按列序对应）；
- 金额一律 `DECIMAL`（浮点会有精度误差，钱的场景必须精确）；
- 统计值（sales/orders/quantity/share）在 Hive 里算好，MySQL 只存不算；
- 行数很小（1~702 行），所以不建索引。

**sys_user 用户表**：
```sql
CREATE TABLE IF NOT EXISTS `sys_user` (
    `id`          BIGINT NOT NULL AUTO_INCREMENT,        -- 自增主键
    `username`    VARCHAR(50)  NOT NULL,                 -- 登录账号
    `password`    VARCHAR(100) NOT NULL,                 -- BCrypt 密文（60字符）
    `nickname`    VARCHAR(50)  DEFAULT NULL,
    `role`        VARCHAR(20)  DEFAULT 'user',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_username` (`username`)                -- 唯一索引：登录查询 + 防重
);
```

`sql/init.sql` 内容与 schema.sql 相同，只是多了建库语句（CREATE DATABASE + USE），供 Navicat 手工执行。

---

# 第二部分：后端 Java 代码

## 4. SupermarketApplication.java —— 启动类

```java
@SpringBootApplication      // 组合注解 = @Configuration + @EnableAutoConfiguration + @ComponentScan
                            // 自动配置 + 扫描当前包及子包的组件
@MapperScan("com.supermarket.mapper")   // 告诉 MyBatis 去哪个包找 Mapper 接口生成代理对象
public class SupermarketApplication {
    public static void main(String[] args) {
        SpringApplication.run(...);     // 启动内嵌 Tomcat，加载整个 Spring 容器
    }
}
```

## 5. GlobalExceptionHandler.java —— 全局异常处理

```java
@RestControllerAdvice       // 拦截所有 Controller 抛出的异常
@Slf4j                      // Lombok 生成 log 对象
public class GlobalExceptionHandler {
```
三个处理方法：
- `MaxUploadSizeExceededException` → 上传超限提示（历史遗留，现无上传功能，保留无害）；
- `IllegalArgumentException` → **业务异常**，把 e.getMessage() 原样返回给前端（比如"用户名或密码错误""非法的表名"），HTTP 200 + `{"success":false,"message":...}` 格式，前端直接弹 message；
- `Exception` → 兜底，返回"系统异常：原因"，并打 error 日志方便排查。

统一返回格式的好处：前端不用判断各种异常类型，一律读 `message` 字段展示。

## 6. 认证四件套（config 包）——重点

### 6.1 AuthInterceptor —— 接口登录拦截器

```java
@Component                 // 交给 Spring 管理
public class AuthInterceptor implements HandlerInterceptor {
```
HandlerInterceptor 是 Spring MVC 的拦截器接口，`preHandle` 在进入 Controller **之前**执行：

```java
HttpSession session = request.getSession(false);
// getSession(false)：没有会话返回 null，而不是新建（避免未登录请求也创建会话）
if (session != null && session.getAttribute(AuthService.SESSION_USER) != null) {
    return true;               // 已登录，放行
}
```
未登录时：
```java
response.setStatus(401);           // HTTP 状态码 401 未认证
response.getWriter().write("{\"success\":false,\"message\":\"未登录或登录已过期，请重新登录\"}");
return false;                      // 不再进入 Controller
```
返回 JSON 而不是默认 HTML 错误页，是为了让前端 `api()` 能解析出 message 并跳登录页。

### 6.2 AuthFilter —— 页面访问过滤器

```java
@Component
public class AuthFilter implements Filter {
```
Filter 是 Servlet 规范接口，比拦截器更底层，能拦到**静态页面**（拦截器只拦 Controller）。

放行清单（这段 if 决定哪些请求不检查登录）：
```java
if (uri.equals("/login.html") || uri.equals("/register.html")   // 登录注册页本身
        || uri.startsWith("/css/") || uri.startsWith("/js/")    // 样式脚本
        || uri.equals("/favicon.ico") || uri.startsWith("/api/auth/")) {
    chain.doFilter(request, response);   // 放行，交给下一个过滤器/目标资源
}
```
已登录 → 放行；未登录：
```java
if (uri.startsWith("/api/")) { chain.doFilter(...); }   // 接口交给拦截器返回 401 JSON
response.sendRedirect("/login.html");                  // 页面 → 302 跳转登录页
```

**分工总结（必背）**：过滤器管页面（重定向），拦截器管接口（401 JSON）。

### 6.3 WebConfig —— 装配

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
```
三个 Bean/配置：
1. `addInterceptors`：注册拦截器，拦 `/api/**`，**放行三个接口**：
   ```java
   .excludePathPatterns("/api/auth/login", "/api/auth/register", "/api/auth/me")
   ```
   login/register 登录前就要调用；me 用于前端检查会话有效性。**改密码/退出不放行**（必须登录）。
2. `authFilterRegistration`：把 AuthFilter 注册为 FilterRegistrationBean 并设 `order=1`（最先执行）。
3. `passwordEncoder()`：提供 `BCryptPasswordEncoder` Bean，供登录比对和注册加密。

### 6.4 DataInitializer —— 默认账号初始化

```java
@Component
public class DataInitializer implements ApplicationRunner {
    public void run(...) {                       // 应用启动完成后自动执行
        if (userMapper.count() > 0) return;      // 已有用户则跳过（幂等）
        admin.setPassword(passwordEncoder.encode("admin123"));   // 明文 → BCrypt 密文
        userMapper.insert(admin);                // 插入 admin 账号
    }
}
```
为什么运行时初始化而不是写死在 SQL：BCrypt 密文必须用编码器动态生成，且这样代码里不出现任何密文。

## 7. entity 与 dto —— 数据对象

### 7.1 User.java
`@Data` 生成所有 getter/setter。5 个字段对应 sys_user 表列：id、username、password（密文）、nickname、role、createTime。

### 7.2 dto 包（14 个类，逐类讲）

**请求类**（前端 → 后端）：
- `LoginRequest`：username、password。
- `RegisterRequest`：username、password、nickname。
- `PasswordRequest`：oldPassword、newPassword。

**响应类**（后端 → 前端）：
- `UserInfo`：id、username、nickname、role。**故意不含 password**——密文永远不出接口。
- `KpiSummary`：totalSales/totalOrders/totalQuantity/avgOrderAmount（来自 result_kpi）+ storeCount/productCount（来自结果表行数统计）。
- `MonthlySales`：month、sales、orders、quantity、avgOrderAmount、growthRate（环比%，首月 null）。
- `CategorySales`：category、sales、orders、quantity、share（占比%，Hive 算好）。
- `DailySales`：day、sales、orders。
- `HourSales`：hour、sales、orders、quantity。
- `WeekdaySales`：dayOfWeek（1=周一…7=周日）、dayName（周一…，Service 层补充）、sales、orders。
- `HourTypeSales`：hour、type（weekday/weekend）、sales、orders。
- `HeatCell`：dayOfWeek、hour、sales（热力图单元格）。
- `StoreSales`：storeId、storeName、city、sales、orders、quantity、avgOrderAmount。
- `CitySales`：city、storeCount、sales、orders、share。
- `ProductSales`：productId、productName、category、price、sales、quantity、orders。
- `StoreDetail`：聚合对象 = overview（门店概况）+ monthly（月度走势）+ categories（品类构成）+ topProducts（热销商品），对应门店详情页一个接口返回的全部数据。
- `StoreInfo`：storeId、storeName、city（下拉框用）。
- `PageResult<T>`：通用分页容器 list/total/page/size。
- `ImportResult` 等旧类已随架构调整删除。

## 8. Mapper 层 —— MyBatis 数据访问

### 8.1 ResultMapper.xml —— 核心（每条 SQL 逐行讲）

所有查询都是 `SELECT ... FROM result_xxx`——**只读结果表**。以几个代表为例：

**① 总览 KPI**
```sql
SELECT total_sales, total_orders, total_quantity, avg_order_amount
FROM result_kpi LIMIT 1
```
Hive 已算好的四个指标直接取一行；LIMIT 1 防意外多行。

**② 月度趋势**
```sql
SELECT month, sales, orders, quantity, avg_order_amount, growth_rate
FROM result_month_sales ORDER BY month
```
按月排序保证折线图时间顺序；growth_rate 是 Hive 用 LAG 窗口函数算的环比。

**③ 指定月份每日走势**
```sql
SELECT day, sales, orders FROM result_daily_sales
WHERE day LIKE CONCAT(#{month}, '%')   -- 如 month='2026-03' → 匹配 '2026-03%'
ORDER BY day
```
`#{}` 预编译占位符，防注入。

**④ 门店详情三连**：门店月度走势 / 门店品类构成 / 门店热销商品，都是 `WHERE store_id = #{storeId}`，其中热销加 `ORDER BY sales DESC LIMIT #{limit}` 取前 10。

**⑤ 时段×工作日**
```sql
SELECT hour, day_type AS type, sales, orders FROM result_hour_weekday_sales ORDER BY hour, day_type
```
注意 `day_type AS type`：列名 day_type 自动驼峰是 dayType，但 DTO 字段叫 type，所以手写别名对齐（实际踩过的坑）。

### 8.2 UserMapper.xml
- `insert`：`useGeneratedKeys="true" keyProperty="id"` 让 MyBatis 回填自增主键。
- `selectByUsername`：登录时按用户名查密文。
- `updatePassword`：改密。

### 8.3 DataMapper.xml —— 动态表名（防注入考点）
```xml
<select id="selectRows" resultType="map">
    SELECT * FROM ${table} LIMIT #{offset}, #{size}
</select>
```
- `${table}` 是字符串拼接——因为**表名不能作为预编译参数**（占位符只能替值不能替表名）；
- 风险与对策：拼接表名有 SQL 注入风险，所以 Service 层做了**白名单校验**（只允许 16 个表名，见 10.7）；
- `resultType="map"`：返回 LinkedHashMap，键是列名——16 张表列各不相同，用 Map 才能通用展示；
- 测试接口 `/api/data/dim_store` 会返回"非法的表名"，就是白名单在拦。

## 9. Service 层 —— 业务逻辑（逐类）

### 9.1 AuthService（重点）

```java
public Map<String, Object> login(LoginRequest req, HttpSession session) {
    User user = userMapper.selectByUsername(req.getUsername().trim());
    if (user == null || !passwordEncoder.matches(req.getPassword(), user.getPassword())) {
        throw new IllegalArgumentException("用户名或密码错误");
    }
```
逐句：按用户名查库 → 用户不存在**或**密码 BCrypt 比对失败 → 抛业务异常（**故意不区分"用户不存在"和"密码错"**，防止撞库探测）。`matches(明文, 密文)` 内部流程：提取密文里的盐 → 用同样的盐重新哈希明文 → 比对结果。

```java
    session.setAttribute(SESSION_USER, info);       // 用户信息存进会话
    session.setMaxInactiveInterval(NORMAL_SECONDS); // 30 分钟失效
    return result;                                  // {success, message, user}
```
Session 原理：服务器生成 JSESSIONID，通过 Cookie 发给浏览器；之后每个请求浏览器自动带回 JSESSIONID，服务器据此找到对应会话——所以后端无状态接口也能"记住"登录状态。

**register**：三步——用户名正则校验（3~20 位字母数字下划线）→ 密码长度≥6 → 查重（"用户名已存在"）→ `passwordEncoder.encode()` 加密入库 → 自动调 login() 完成自动登录。

**changePassword**：按会话中的用户 id 查库 → `matches(旧密码, 密文)` 验证 → 新密码加密后 update。旧密码比对是防"捡到已登录浏览器就改密码"。

### 9.2 DashboardService
- `getKpi()`：查 result_kpi；**为 null（结果表空）时手动补全 0 值**——空库时页面正常显示 0 而不是报错；storeCount/productCount 用 `COUNT(*)` 统计结果表行数得到。
- `getStoreTop(limit)`：结果表已按销售额降序，Java 里 `subList(0, min(limit, size))` 截前 N，防止越界。
- 其余方法都是"查结果表 → 返回"。

### 9.3 HourService
- `getWeekday()`：把数据库 7 行补成**完整 7 天**（数据缺失的天补 0，图表好看）；同时填 dayName（dayOfWeek 1→"周一"…7→"周日"）。用 `Collectors.toMap` 把列表转 Map 便于按天取值。

### 9.4 MonthlyService
- `getDaily(month)`：先用 `YearMonth.parse` 校验格式（非法输入抛"月份格式不正确"），再查 result_daily_sales。格式校验防脏参数进 SQL。

### 9.5 StoreService
- `getDetail(storeId)`：先从门店排行里找该店概况（`orElseThrow` 找不到就报"结果表中不存在该门店"——这种错误提示直接告诉用户去检查 Hive/Sqoop）；再并行查三个下钻表组装 StoreDetail。

### 9.6 ProductService
- `getList()`：一次返回全部 39 行；筛选/排序/畅销/滞销**全部交给前端**——数据量小，前端内存操作即时响应，后端保持极简。

### 9.7 DataService（防注入考点）
```java
private static final Map<String, String> TABLES = new LinkedHashMap<>();
static { TABLES.put("result_kpi", "总览 KPI"); ... }   // 16 个表名白名单
```
```java
public PageResult<Map<String,Object>> getData(String table, int page, int size) {
    if (!TABLES.containsKey(table)) {
        throw new IllegalArgumentException("非法的表名：" + table);   // 白名单拦截
    }
    ...
}
```
因为 DataMapper 里用了 `${table}` 拼接，**白名单就是最后一道防线**——只要表名不在 16 个之内，任何拼接注入都无法执行。

## 10. Controller 层 —— 接口清单（逐类）

| 控制器 | 接口 | 作用 |
|---|---|---|
| AuthController | POST /api/auth/login | 登录（免登录） |
| | POST /api/auth/register | 注册并自动登录（免登录） |
| | GET /api/auth/me | 当前用户（免登录，返回 null 表示未登录） |
| | POST /api/auth/password | 修改密码（需登录） |
| | POST /api/auth/logout | 退出（需登录） |
| DashboardController | /summary /monthly /category-share /store-top /hourly | 总览页数据 |
| CategoryController | /overview /trend /top-products /categories | 品类页数据 |
| HourController | /distribution /weekday /weekday-compare /heatmap | 时段页数据 |
| MonthlyController | /trend /daily | 月度页数据 |
| StoreController | /ranking /cities /city-trend /stores /{id}/detail | 门店页数据 |
| ProductController | /list /categories | 商品页数据 |
| DataController | /tables /{table} | 结果表浏览 |

**以 AuthController.login 为例讲透请求流转**：
```java
@PostMapping("/login")                       // 只接受 POST（登录用 GET 会泄露密码到 URL）
public Map<String, Object> login(@RequestBody LoginRequest req, HttpSession session) {
    return authService.login(req, session);
}
```
`@RequestBody`：把前端 JSON（`{"username":"admin","password":"admin123"}`）反序列化成 LoginRequest 对象；`HttpSession` 由 Spring 自动注入当前请求的会话。返回 Map，Jackson 自动序列化成 JSON 给前端。

**以 DataController.data 为例讲路径参数**：`@GetMapping("/{table}")` + `@PathVariable String table` 把 URL 里的表名取出来；`@RequestParam(defaultValue="1")` 提供默认页码。

---

# 第三部分：前端代码

## 11. css/style.css —— 样式要点（讲区块即可）

- `:root` 里定义 CSS 变量（主色 #4f7cff、侧边栏深蓝渐变），全站统一配色；
- `.sidebar` 固定定位左侧 230px 深色导航 + `.main` margin-left 让出位置 —— 经典后台管理布局；
- `.kpi-grid` + `.kpi-card`（蓝/绿/橙/紫/青/粉六种渐变）：总览页 6 张指标卡；
- `.row/.col-8/.col-4/.col-6`：flex 栅格实现图表两列/三列布局；
- `.chart` 固定高度 360px 给 ECharts 画布；
- `.empty-overlay` 绝对定位遮罩：数据为空时盖在图表上显示"暂无数据"；
- `.toast` 右上角弹窗提示；`.auth-body/.auth-card` 登录注册页居中卡片；
- `.upload-zone` 等导入页样式（已随架构调整弃用，可删除）。

## 12. js/common.js —— 公共脚本（每个函数逐讲）

```js
const PALETTE = ['#4f7cff', ...];    // 12 色图表统一配色
const ECHARTS = {};                  // 保存所有图表实例，供 resize 用
let CURRENT_USER = null;             // 当前登录用户
```

- `api(url, options)`：统一请求封装。
  ```js
  if (resp.status === 401) { location.href = '/login.html'; ... }
  ```
  **401 统一处理**：任何接口返回未登录，直接跳登录页——这就是"会话过期自动踢回登录页"的实现；
  其他错误把后端 JSON 里的 message 提出来抛 Error，页面 catch 后 toast 展示。
- `postApi(url, body)`：POST JSON 的快捷封装（自动加 Content-Type 头）。
- `initChart(el, option)`：**全局图表入口**。两件事：① 给每个图表注入 `toolbox.saveAsImage` 工具栏（所有图表一键导出 PNG 的实现点）；② `echarts.init` 初始化 + `setOption` 渲染 + 存实例。
- `showEmptyChart/hideEmptyChart`：在图表容器上盖/揭"📭 暂无数据"遮罩。
- `showEmptyBanner`：结果表全空时在页面顶部插一条黄色横幅，引导用户去 Hive+Sqoop。
- `ensureData()`：先调 /api/dashboard/summary 判断有没有数据，没数据就显示横幅，有数据才继续加载页面。
- `fmtMoney(v)`：金额格式化——≥1 亿转"亿"、≥1 万转"万"、否则千分位两位小数，例如 57084290.95 → "¥5708.4万"。
- `fmtInt(v)`：整数千分位（85,292）。
- `esc(s)`：HTML 转义（& < > " ' 五兄弟），防止商品名等数据库内容注入 HTML。
- `toast(msg, type)`：右上角 3.5 秒自动消失的提示。
- `monthLabel(m)`："2026-03" → "2026年03月"。
- `renderUser()`：把 CURRENT_USER 昵称写进顶栏 `#userBox`。
- `logout()`：调退出接口后跳登录页。
- `checkAuth()`：**页面级鉴权**——带 `data-page` 属性的页面加载时调 /api/auth/me：成功 → 渲染用户名；失败 → api() 已自动跳登录页。登录/注册页没有 data-page 属性所以不检查。
- 底部 IIFE（立即执行函数）：① 按 `body[data-page]` 高亮侧边栏当前项；② 顶栏显示今天日期；③ 窗口 resize 时所有图表自适应。

## 13. login.html / register.html —— 登录注册页

- 无侧边栏的居中卡片布局（`auth-body` 渐变背景）。
- `doLogin()`：读输入 → 空值校验 → 按钮置灰防连点 → `postApi('/api/auth/login', {username, password})` → 成功 toast + 300ms 后跳 index.html；失败把 message 显示在红色提示框。
- 密码框监听 Enter 键直接登录。
- 页面加载时先调 /api/auth/me，**已登录直接进首页**（避免重复登录）。
- register.html 的 `doRegister()`：前端先做四道校验（非空/用户名正则/密码长度/两次一致），通过才发请求——**前端校验防呆，后端校验保安全**。

## 14. index.html —— 数据总览（图表逐块讲）

加载流程：`loadAll()` 先 `ensureData()`（空库直接出横幅）→ `Promise.all` 并行发 5 个请求（并行比串行快 5 倍）→ 分别渲染。

`renderKpis(kpi)`：把 6 个指标卡配成数组，用模板字符串生成 HTML——每张卡 = 渐变类 + 图标 + 数值（fmtMoney/fmtInt 格式化）+ 说明文字。

`renderMonthly(list)` 的 ECharts 配置逐块：
- `xAxis: category` 横轴是 6 个月份；
- `yAxis` 数组里**两个轴**：左轴销售额（formatter 把元转"万"），右轴订单数——双轴图让柱子和折线各用各的刻度；
- `series[0]` bar：销售额柱，`itemStyle.color` 用线性渐变（顶部浅蓝→底部深蓝），`borderRadius` 圆角柱顶；
- `series[1]` line：订单数折线，`yAxisIndex: 1` 挂右轴，`smooth` 平滑曲线。

`renderCategory(list)` 环形饼图：`radius: ['42%','70%']` 内外半径差出环形；tooltip 显示"品类：金额（百分比）"；`emphasis` 悬停放大显示标签。

`renderHourly(list)` 柱线组合：先用 `reduce` 找出销售额最大的 hour 作为高峰；`itemStyle.color` 是**函数**——`p.dataIndex === list.indexOf(peak)` 时高峰柱子染橙色，其余蓝色；`markPoint` 在最大值处标"高峰"字样。

`renderStores(list)` 横向条形图：`yAxis inverse: true` 让第一名排最上面；每根条 `label` 在右侧显示金额；渐变蓝→绿。

## 15. category.html / hourly.html / monthly.html / store.html / product.html —— 各页要点

**category.html**
- `renderTrend`：品类×月份多系列折线——`[...new Set(...)]` 从二维结果里提取去重的月份数组和品类数组，再为每个品类建一条 series，按月份 map 找对应销售额（没有就 0）；
- 品类下拉切换热销商品表（top-products 接口）。

**hourly.html**
- `renderInsights`：前端从分布数据算出**高峰/次高峰时段、最旺星期、工作日:周末销售比**四张洞察卡——轻量派生指标在前端算即可；
- `renderHeatmap`：ECharts heatmap 类型，数据格式 `[hour, dayOfWeek-1, sales]`；`visualMap` 把 0~最大值映射成白→深蓝五档色，颜色越深销售额越高。

**monthly.html**
- `renderGrowth`：环比柱状图 `itemStyle.color` 是函数——值为正染绿、为负染红（涨绿跌红）；
- 月份下拉 → `/api/monthly/daily?month=xxx` 画每日走势，再算日均值画一条虚线"日均线"。

**store.html**
- 城市消费力（柱+环形图）、城市×月份多系列折线、18 店全量排行榜（前三名金/银/铜徽章 `rank-1/2/3`）；
- 门店详情：`loadDetail()` 一个接口拿 StoreDetail，一次渲染 4 块（KPI 卡/月度走势/品类构成/热销 TOP10）。

**product.html**
- 一次取全部 39 行 → `filtered()` 前端完成关键词过滤+品类筛选+六种排序 → `renderTable()` 纯前端分页（每页 10 行）；
- 畅销 TOP10 = 按 sales 降序取 10，滞销 TOP10 = 升序取 10，同一个 horizontalBar 渲染函数复用。

**data.html**
- `COLUMNS` 对象：16 张表的"列名→中文表头"映射（如 total_sales → 总销售额）；
- `loadData()`：表头用第一行数据的键顺序 + COLUMNS 映射生成；数值列右对齐，NULL 显示灰色 "NULL"；分页按钮边界禁用。

---

# 第四部分：虚拟机侧脚本

## 16. hive/load-data.sql —— 原始数据装载

```sql
LOAD DATA LOCAL INPATH '/root/data/orders_2026-01.txt' INTO TABLE fact_order_detail;
```
逐词讲：LOAD DATA 装载命令；LOCAL 表示文件在**本机**（不是 HDFS）；INPATH 后是文件路径；INTO TABLE 追加到目标表。6 个月文件依次追加进同一张表，最后表尾有验证查询（应 267822 行）。**注意重复执行会重复装载**，所以只跑一次。

## 17. hive/hive-analysis.sql —— 核心分析（每条 SQL 逐行讲）

**第 0 步建原始表**（以事实表为例）：
```sql
CREATE TABLE IF NOT EXISTS fact_order_detail (
    order_id BIGINT, product_id INT, store_id INT,
    quantity INT, amount DECIMAL(12,2), order_time STRING
) ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t';
```
`ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t'`：告诉 Hive 数据文件按 Tab 切列——和 txt 文件格式对应。

**① KPI（1 行）**：
```sql
SELECT sum(amount), count(DISTINCT order_id), sum(quantity),
       round(sum(amount)/count(DISTINCT order_id), 2)
FROM fact_order_detail;
```
四个聚合：总销售额、**去重**订单数（一个订单多行，COUNT DISTINCT 才算真订单数）、总销量、客单价（销售额÷订单数，round 保留两位）。

**② 品类占比（窗口函数，必考题）**：
```sql
round(sum(f.amount) / sum(sum(f.amount)) OVER () * 100, 2) AS share
```
`sum(sum(f.amount)) OVER ()`：OVER() 空窗口 = 在整个结果集上再聚合一次。内层 sum 按 category 分组求和，外层 sum 对"所有分组的和"再求和 = 全局总销售额。于是每个品类行都能算 `本类/全局*100`。JOIN 商品表是为了把 product_id 翻译成 category。

**③ 月度环比（LAG，必考题）**：
```sql
round((t.sales - lag(t.sales,1) OVER (ORDER BY t.month)) / lag(t.sales,1) OVER (ORDER BY t.month) * 100, 2) AS growth_rate
```
`lag(sales,1) over(order by month)`：按月份排序后取**上一行**的销售额（lag=往后看 1 行）。本月-上月 再除以上月 = 环比。首月没有上一行 → NULL。外层套子查询是因为窗口函数不能和 GROUP BY 同层使用（先分组再开窗）。

**④ 星期计算**：
```sql
pmod(datediff(to_date(order_time), '2026-01-05'), 7) + 1 AS day_of_week
```
`datediff` 算每个日期与 2026-01-05（周一）相差几天；`pmod(x,7)` 对 7 取模（pmod 保证结果非负）；+1 得到 1=周一…7=周日。

**⑤ 滞销商品（LEFT JOIN，必考题）**：
```sql
FROM dim_product p LEFT JOIN fact_order_detail f ON p.product_id = f.product_id
...
coalesce(sum(f.amount), 0) AS sales, count(f.product_id) AS orders
```
LEFT JOIN 让**没卖过的商品也保留一行**（右表全 NULL）；`coalesce(x, 0)` 把 NULL 换成 0（没卖过=销售额 0）；`count(f.product_id)` 只数非 NULL，所以没卖过的 orders=0。这就是"滞销榜含零销售商品"的原理。

**⑥~⑯ 其余表**：都是同一模式——`GROUP BY 维度组合` + 聚合，如门店×月份就是 `GROUP BY store_id, substr(order_time,1,7)`。

## 18. hive/sqoop-export.sh —— 导出脚本（逐段讲）

```bash
HOST_IP="192.168.88.1"        # 宿主机 VMnet8 地址（ipconfig 查）
TABLES="result_kpi ..."       # 16 张表清单
```
**前置检查 1**：一条 `hive -S -e` 用 UNION ALL 一次查完 16 张表的行数——有表不存在/全为 0 直接退出并提示（防"没 LOAD 就导出"）。
**前置检查 2/3**：`command -v mysql` / `command -v sqoop` 检查命令存在；sqoop 不在 PATH 时自动遍历常见安装目录。
**第 1 步 TRUNCATE 循环**：导出前清空 MySQL 目标表——export 是追加写，不清空会重复；失败时区分"表不存在"和"连不上"两种提示。
**第 2 步导出循环**（核心）：
```bash
sqoop export \
  --connect "jdbc:mysql://$HOST_IP:3306/$MYSQL_DB?..." \   # 目标库
  --username "$MYSQL_USER" --password "$MYSQL_PASS" \
  --table "$t" \                                           # MySQL 目标表
  --export-dir "/user/hive/warehouse/$HIVE_DB.db/$t" \     # Hive 表在 HDFS 的目录
  --input-fields-terminated-by '\001' \                    # Hive 默认列分隔符
  --input-null-string '\\N' --input-null-non-string '\\N' \# 把 Hive 的 \N 当 NULL
  --num-mappers 1                                          # 单任务顺序写，避免并行乱序
```
原理：Sqoop 启动一个 MapReduce 任务，Mapper 读 HDFS 文件每行按 \001 切列、拼成 INSERT 批量写 MySQL。

---

# 第五部分：踩坑记录（答辩加分素材）

1. **JDK 24 + Lombok**：新版 javac 不再自动发现注解处理器 → pom 显式声明 annotationProcessorPaths。
2. **Windows 运行 jar 时文件被锁**：java -jar 运行中无法重新打包 → 先杀进程再 mvn package。
3. **Sqoop 1.4.7 + Hadoop 3.3**：缺 commons-lang-2.6（NoClassDefFoundError）→ 从 Hive lib 拷 jar；HADOOP_CONF_DIR 缺失报 Cannot initialize Cluster；hcatalog 兼容性差 → 改用 --export-dir。
4. **day_type 列名映射**：下划线转驼峰得到 dayType 而非 DTO 的 type → SQL 手写别名对齐。
5. **虚拟机连宿主机 MySQL**：连的是虚拟机自己的 IP（HOST_IP 配错）→ 宿主机 VMnet8 是 192.168.88.1；root 只允许 localhost 登录 → 建 root@'192.168.88.%'；Windows 防火墙挡 3306 → 加放行规则。
6. **拦截器放行范围过大**：/api/auth/** 全放行导致改密接口未登录可调 → 只放行 login/register/me 三个。
7. **结果表空 vs 有数据**：KPI 查询空表返回 null → Service 层补 0，前端 ensureData 出横幅引导，两层兜底。
